[package]
name = "mir_reader"
version = "0.0.0"
authors = ["DSPOM"]
edition = "2021"
license="GPL-3.0"

[lib]
doctest = false


[dependencies]
mir = {version = "0.0.0", path = "../mir" }
bforest = {version = "0.0.0" , path = "../../lib/bforest"}
lasso = {version = "0.6", features = ["ahash"]}

[dev-dependencies]
expect-test = "1.1"
