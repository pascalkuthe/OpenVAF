[package]
name = "const_eval"
version = "0.0.0"
authors = ["DSPOM"]
edition = "2021"
license = "GPL-3.0"

[lib]
doctest = false

[dependencies]
cfg = { version = "0.0.0", path = "../cfg" }
data_flow = { version = "0.0.0", path = "../data_flow" }

ahash = "0.7"

[dev-dependencies]
expect-test = "1.1"
