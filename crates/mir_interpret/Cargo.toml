[package]
name = "mir_interpret"
version = "0.0.0"
authors = ["DSPOM"]
edition = "2021"
license="GPL-3.0"

[lib]
doctest = false


[dependencies]
mir = {version = "0.0.0", path = "../mir" }

stdx = {version = "0.0.0", path = "../stdx" }
list_pool = {version = "0.0.0" , path = "../list_pool"}

typed-index-collections = "3.0"
lasso = {version = "0.6", features = ["ahash"]}
